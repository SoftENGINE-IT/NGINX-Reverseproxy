# Catch-All Configuration for undefined domains
# Generated by NRP v2.0

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;  # Fängt alle nicht definierten Domains ab

    root {{ nginx_html_dir }};  # Standard-Root-Verzeichnis für die 404-Seite
    index 404.html;

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}

server {
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;

    server_name _;  # Fängt alle nicht definierten Domains ab

    ssl_certificate {{ dummy_cert }};
    ssl_certificate_key {{ dummy_key }};

    root {{ nginx_html_dir }};  # Standard-Root-Verzeichnis für die 404-Seite
    index 404.html;

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
