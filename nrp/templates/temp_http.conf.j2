# Temporary HTTP Configuration for {{ fqdn }}
# Used for LetsEncrypt certificate generation
# Generated by NRP v2.0

server {
    listen 80;
    server_name {{ fqdn }};

    location / {
        return 301 https://$host:{{ external_port }}$request_uri;
    }
}
